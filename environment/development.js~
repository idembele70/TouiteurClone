const { resolve } = require("path");
module.exports = {
  db: {
    protocol: "PROTOCOL",
    user: "USER",
    password: "PASSWORD",
    host: "HOST",
    name: "NAME",
    options: "OPTIONS",
  },
  hashRounds: HASH_ROUNDS,
  jwt : {
    secret : 'SECRET',
  },
  ssl: {
    cert: resolve("ssl", "CRT_FILE"),
    key: resolve("ssl", "KEY_FILE"),
  },
  ports: {
    http: PORT_HTTP,
    https: PORT_HTTPS,
  },
};
